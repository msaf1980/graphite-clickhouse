[common]
listen = "127.0.0.1:{GCH_PORT}"
max-cpu = 0

[clickhouse]
url = "http://localhost:{CH_PORT}"
data-table = "graphite"
index-table = "graphite_index"
index-timeout = "1m"

tagged-table = "graphite_tags"
tagged-autocomplete-days = 1
data-timeout = "15s"

[[data-table]]
table = "graphite"

[logging]
